enum EnvEnum { dev, prod }

enum AppTheme { lightTheme, darkTheme }

enum ToastEnum { error, warning, success, info }

enum ResponsiveDeviceType { col2, col4, col6, col8, col10, col12 }

enum AbsNormalStatus { initial, loading, error, success }

enum TabType { normal, box }

enum PermissionFor {
  camera,
  gallery,
  microPhone,
  storage,
  contact,
  location,
}

enum MsgStatus {
  open,
  closed,
  onHold,
  processing,
  reopened,
  archived,
  resolved,
  paymentResolved
}

enum AttachmentType { image, pdf, doc, audio, video }
